---
version: 5

defaults:
  datadir: "hieradata"

hierarchy:
  - name: 'Yaml backend'
    data_hash: yaml_data
    paths:
      - "/etc/puppetlabs/code/environments/production/hieradata/nodes/%{trusted.certname}.yaml"
      - 'common.yaml'
