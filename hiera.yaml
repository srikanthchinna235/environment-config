---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: 'Yaml backend'
    data_hash: yaml_data
    paths:
      - "/etc/puppetlabs/code/environments/development/hieradata/nodes/%{trusted.certname}.yaml"
      - 'common.yaml'
